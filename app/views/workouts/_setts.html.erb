<table class="w-full leading-10">
    <thead class="text-left border-b">
    <tr class="pb-1">
        <th>Lift</th>
        <th>Weight</th>
        <th>Reps</th>
        <th>Volume</th>
        <th>Date</th>
        <th>&nbsp;</th>
    </tr>
    </thead>

    <tbody>
    <% if setts.any? %>
        <% setts&.each do |sett| %>
            <%= render partial: 'sett', locals: { sett: sett } %>
        <% end %>
    <% end %>
    </tbody>
</table>
