<!DOCTYPE html>
<html>
  <head>
    <title>Gym App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="bg-gray-600 text-gray-900 tracking-wider leading-normal">
    <%= render "shared/header" %>
    <% if notice %>
      <div class="flex justify-end">
        <p id="notice" class="mx-4 my-4 whitespace-nowrap inline-flex px-4 py-2 border border-transparent rounded-md shadow-sm text-base font-medium text-white bg-green-300 hover:bg-green-400"><%= notice %></p>
      </div>
    <% end %>

    <div class="container mx-auto full my-4">
      <%= yield %>
    </div>
  </body>
</html>
