<p id="notice"><%= notice %></p>

<%= turbo_frame_tag dom_id(@comment) do %>
  <%= render partial: "comment", locals: { user: @user, workout: @workout, comment: @comment } %>
<% end %>

<%= link_to 'Edit', edit_comment_path(@comment) %> |
<%= link_to 'Back', comments_path %>
